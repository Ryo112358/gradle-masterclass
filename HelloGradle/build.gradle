logger.info ">>> ${relativePath(buildFile)} -> Global build script reached!";
logger.info ">>> build.gradle: ${project.gradle.gradleUserHomeDir}";
logger.info ">>> build.gradle: $gradle.gradleHomeDir";


logger.info """
***********************************************************
Welcome to Gradle $gradle.gradleVersion - http://www.gradle.org
Gradle home is set to: $gradle.gradleHomeDir
Gradle user directory is set to: $gradle.gradleUserHomeDir

Running script: ${project.relativePath(project.buildFile)}
Base directory: $projectDir
***********************************************************
"""



if (project.hasProperty('earthNationCapital')) {
	logger.info "The capital of the Earth Kingdom is ${earthNationCapital}!\n";
} else {
	logger.info "The earth nation has fallen... ~.~\n";
}



if (hasProperty('waterNationCapital')) {
	logger.info "The capital of the Water Tribe is ${waterNationCapital}!\n";
} else {
	logger.info "The Water Tribe has fallen... ~.~\n";
}